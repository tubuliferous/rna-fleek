# RNA-FLEEK dependencies
# Install: pip install -r requirements.txt
# Or: pip install .  (uses pyproject.toml)

# Core
anndata>=0.9
numpy>=1.21
scipy>=1.7

# Analysis (embeddings, DEG, clustering)
scanpy>=1.9
scikit-learn
matplotlib
igraph
leidenalg

# SSL (needed for Claude API calls in bundled app)
certifi

# PaCMAP embedding (optional — FLEEK works without it)
pacmap<0.8

# Pseudo-bulk DEG (optional — falls back to t-test without it)
# pydeseq2
# statsmodels  # needed for t-test fallback BH correction

# Build only (not needed to run FLEEK)
# hatchling
# pyinstaller
