# RNA-FLEEK dependencies
# Install: pip install -r requirements.txt
# Or: pip install .  (uses pyproject.toml)

# Core
anndata>=0.9
numpy>=1.21
scipy>=1.7

# Analysis (embeddings, DEG, clustering)
scanpy>=1.9
scikit-learn
matplotlib
igraph
leidenalg

# SSL (needed for Claude API calls in bundled app)
certifi

# PaCMAP embedding (optional — FLEEK works without it)
pacmap<0.8

# Pseudo-bulk DEG — pyDESeq2 is preferred; statsmodels is required for the
# t-test fallback's Benjamini-Hochberg correction. Both are bundled into the
# desktop-app builds so pseudo-bulk DEG uses DESeq2 out of the box.
pydeseq2
statsmodels

# Threadsafe numba threading backend. Intel only ships TBB wheels for
# Linux + Windows x86_64; on macOS the server uses OMP (libomp from
# Homebrew) and a single-threaded fallback otherwise. The platform
# marker keeps pip from erroring on macOS / arm64 hosts where no wheel
# exists. server.py auto-selects which layer to use at runtime.
tbb; sys_platform != "darwin" and platform_machine == "x86_64"

# Build only (not needed to run FLEEK)
# hatchling
# pyinstaller
